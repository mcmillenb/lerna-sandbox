name: lerna

on:
  push:
    branches: [ develop ]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: thanksd/lerna-action@v0.0.17
        with:
          hash: ${{ hashFiles('package-lock.json') }}
