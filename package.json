{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "pub:pre": "npx lerna publish prerelease -y",
    "pub:patch": "npx lerna publish patch -y"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/thanksd/lerna-sandbox.git"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.11.5",
    "babel-jest": "^26.3.0",
    "jest": "^26.4.2",
    "lerna": "^3.22.1",
    "rollup": "^2.28.2"
  }
}
