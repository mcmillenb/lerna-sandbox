{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "pub:pre": "lerna publish prerelease -y",
    "pub:patch": "lerna publish patch -y"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/thanksd/lerna-sandbox.git"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.11.5",
    "babel-jest": "^26.3.0",
    "jest": "^26.4.2",
    "lerna": "^3.22.1",
    "rollup": "^2.27.1"
  }
}
